# Node-puppeteer

# clone the project
git clone https://github.com/wqs2019/Node-puppeteer.git

# enter the project directory
cd Node-puppeteer

# install dependency
npm install

# develop
node index.js
